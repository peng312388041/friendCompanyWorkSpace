package util;

import iservice.IPersonService;
import model.Person;

import org.apache.log4j.PropertyConfigurator;

public class Main {

	public static void main(String[] args) {
		PropertyConfigurator.configure("log4j.properties ");
		IPersonService iPersonService
		// Logger logger = Logger.getLogger(ImplPersonDao.class);
		// logger.debug(null);

		// System.out.println(service.getCount());

		// System.out.println(service.getAll());

		// System.out.println(service.get(12));

		// Person person = service.get(12);
		// person.setPerson_name("まさか");
		// service.update(person);

		Person person = new Person();
		person.setPerson_name("chong");
		person.setPerson_age(15);
		iPersonService.insert(person);

		// System.out.println(service.delete(26));
		// ClassPathXmlApplicationContext context = null;
		// try {
		// context = new ClassPathXmlApplicationContext(new String[] {
		// "beans.xml" });
		// } catch (Exception e) {
		// e.printStackTrace();
		// context.close();
		// }
		// IPersonService service = context.getBean(ImplPersonService.class);
		// System.out.println(service.getAll());

	}
}
