package model;

public class Main {
	public static final int a = 1;
	 
	public static void main(String[] args) {
		Runnable runnable=new Seller("A");
		new Thread(runnable).start(); 
		new Thread(runnable).start(); 
		new Thread(runnable).start(); 
		new Thread(runnable).start(); 
 
		
	}

}
